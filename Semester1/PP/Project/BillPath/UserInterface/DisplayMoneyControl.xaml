<UserControl x:Class="BillPath.UserInterface.DisplayMoneyControl"
             x:Name="_self"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:BillPath.UserInterface"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="400">
    <StackPanel Orientation="Horizontal">
        <TextBlock FontSize="{Binding FontSize, ElementName=_self}"
                   Foreground="{Binding Foreground, ElementName=_self}"
                   Text="{Binding Sum, ElementName=_self}" />
        <TextBlock FontSize="{Binding FontSize, ElementName=_self}"
                   Foreground="{Binding Foreground, ElementName=_self}"
                   Text="{Binding CurrencySymbol, ElementName=_self}" />
    </StackPanel>
</UserControl>